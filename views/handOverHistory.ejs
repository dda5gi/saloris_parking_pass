<!doctype html>
<html lang="en">
  <%-include('head.ejs') %>
  <body>
    <%-include('nav.ejs') %>
    <li class="list-group-item">
      <h2 class="accordion-header">양도 내역 조회</h2>
    </li>
    <% if(history != 'empty') {%>
    <% for(var i=0; i<history.length; i++) { %>
      <div class="accordion-item">
        <h2 class="accordion-header" id="heading_<%=i+1 %>">
          <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapse_<%=i %>" aria-expanded="false" aria-controls="#collapse_<%=i %>">
            인수자 : <%= history[i].to %>
          </button>
        </h2>
        <div id="collapse_<%=i %>" class="accordion-collapse collapse" aria-labelledby="heading_<%=i+1 %>" data-bs-parent="#accordionExample">
          <div class="accordion-body">
            <ul class="list-group">
              <li class="list-group-item">시작 시간 : <%= history[i].startTime.toLocaleString() %></li>
              <li class="list-group-item">종료 시간 : <%= history[i].endTime.toLocaleString() %></li>
              <li class="list-group-item">
                <button class="btn btn-info" id="tx<%=i %>" onclick=location.href='https://baobab.scope.klaytn.com/tx/<%=history[i].txHash %>'>트랜잭션 확인</button>
              </li>
            </ul>
          </div>
        </div>
      </div>
    <% }}else{ %>
      <div>양도 이력이 존재하지 않습니다.</div>
    <% } %>

    <!-- SCRIPT-SCRIPT-SCRIPT-SCRIPT-SCRIPT-SCRIPT-SCRIPT-SCRIPT-SCRIPT-SCRIPT- -->
    <script>
    </script>

  </body>
  <style type="text/css">
    body {
      background-color: #dddddd;
    }
  </style>
</html>