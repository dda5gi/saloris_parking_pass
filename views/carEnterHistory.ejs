<!doctype html>
<html lang="en">
  <%-include('head.ejs') %>
  <body>
    <%-include('nav.ejs') %>
    <h2 class="display-4">입차 트랜잭션</h2>
    <hr class="my-4">

    <div class="container">
    <% if(history != 'empty') {%>
    <% for(var i=0; i<history.length; i++) { %>
      <div class="accordion-item">
        <h2 class="accordion-header" id="heading_<%=i+1 %>">
          <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapse_<%=i %>" aria-expanded="false" aria-controls="#collapse_<%=i %>">
            <%= history[i].date.toLocaleString() %>
          </button>
        </h2>
        <div id="collapse_<%=i %>" class="accordion-collapse collapse">
          <div class="accordion-body" style="background-color: #e7f0ff">
            <ul class="list-group">
              <li class="list-group-item">차량 번호 : <%= history[i].carNumber %></li>
              <li class="list-group-item">
                <button class="btn btn-info" id="tx<%=i %>" onclick=location.href='https://baobab.scope.klaytn.com/tx/<%=history[i].txHash %>'>트랜잭션 확인</button>
              </li>
            </ul>
          </div>
        </div>
      </div>
    <% }}else{ %>
      <div>입차 이력이 존재하지 않습니다.</div>
    <% } %>
  </div>
  </body>
  <!-- SCRIPT-SCRIPT-SCRIPT-SCRIPT-SCRIPT-SCRIPT-SCRIPT-SCRIPT-SCRIPT-SCRIPT- -->
  <script>
  </script>
</html>